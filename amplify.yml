version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cp studio.edn site/studio.edn
        - cd site
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: site/dist
    files:
      - '**/*'
  cache:
    paths:
      - site/node_modules/**/*
